import 'package:flutter/material.dart';

class Myfilter extends StatelessWidget {
  static const String Routename = 'filter';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("I am in filter"),
    );
  }
}
